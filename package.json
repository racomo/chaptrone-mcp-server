{
  "name": "chaptrone-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP tools + HTTP bridge for Goose to interact with Chaptr One backend.",
  "engines": { "node": "20.x" },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
