{
  "name": "chaptrone-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP tools for Goose to interact with Chaptr One backend.",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p . || echo \"no tsconfig found, skipping build\"",
    "start": "node dist/server.js || node src/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.3.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  }
}
